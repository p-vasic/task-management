import { Box } from "@mui/material";
import { FC } from "react";
import Header from "../components/Header";

const Edit: FC = () => {
  return (
    <Box>
      <Header title="Edit" />
    </Box>
  );
};

export default Edit;
